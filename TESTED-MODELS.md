# Tested Models

Generated by `npm run harness` on 2026-06-19T06:34:17.933Z. Per-target overall pass rate across the model-dependent battery (e2e, simulate, large, false-denial). A target is **supported** at ≥ 80% overall — but only targets that actually RAN the battery count. A target that could not be reached (model 404 / no key / can't produce strict scope JSON) is **not measured**, not "100%".

This is a MEASUREMENT, not a 100%-green claim. Weaker models are documented here as best-effort, not chased by overfitting the pipeline.

| Model | overall | rate | status |
|---|---|---|---|
| minimax-m3@ollama | 65/65 | 100% | recommended |

**Recommended:** minimax-m3@ollama (65/65, 100%).
